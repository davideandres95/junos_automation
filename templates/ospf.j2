protocols {
    ospf {
        {% for area in ospf.areas %}
        area {{ area.id }} {
            {% for interface in area.interfaces %}
                interface {{ interface.name }};
            {% endfor %}
        }
        {% endfor %}
    }
} 
